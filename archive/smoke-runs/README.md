Local Playwright smoke runs write their temporary SQLite database, failure screenshots, and downloaded reports here.

These per-run artifacts are intentionally archived instead of deleted. Individual timestamped run directories are ignored by git.
