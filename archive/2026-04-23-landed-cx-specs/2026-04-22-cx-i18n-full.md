# CX Task: Полная i18n интеграция — react-i18next, ru.json, Accept-Language на backend

## Goal
Довести i18n scaffold в `D:\AB_TEST\` до реальной переключаемой локализации: подключить `react-i18next`, вынести все UI-строки в `en.json`, добавить `ru.json` с полным переводом, научить backend читать `Accept-Language` для report/export, добавить language switcher в header.

## Context
- Репо: `D:\AB_TEST\`, `main`, HEAD после v1.0.0. Не ветка, не push.
- Scaffold уже landed: `app/frontend/src/i18n/index.ts`, `app/frontend/src/i18n/en.json` (66 строк). `en.json` содержит **неполный** набор строк — только часть UI.
- `react-i18next` в deps ещё нет — добавить.
- Backend: отчёты (`services/export_service.py` markdown/html/pdf), warning-тексты в `rules/catalog.py`, error messages в `schemas/api.py` — сейчас только English. Backend должен принимать `Accept-Language: ru` и возвращать переведённые сообщения.
- Verify на HEAD зелёный: 165 backend, 184 frontend unit, a11y axe coverage, lighthouse gate a11y=1.0.
- Целевая аудитория — ru (основной юзер), en (существующий). de/es — не в scope.

## Deliverables
1. **Frontend i18n framework:**
   - `npm install --prefix app/frontend react-i18next i18next i18next-browser-languagedetector`
   - Обновить `app/frontend/src/i18n/index.ts`: инициализация i18next с language detector (URL param > localStorage > browser > fallback en), namespace `common`.
   - Подключить в `app/frontend/src/main.tsx` до render.
   - Все `<Trans>`/`t()` должны работать без SSR.

2. **Полный `en.json`:**
   - Извлечь все текстовые строки из компонентов: `WizardPanel`, `WizardDraftStep`, `WizardReviewStep`, `SidebarPanel`, `ResultsPanel`, все `components/results/*`, `TemplateGallery`, `ShortcutHelp`, `ProjectListFilters`, `ToastSystem`, `ErrorBoundary`/`ChartErrorBoundary`, `ChartExport`, `EmptyState`, `InlineConfirmButton`.
   - Nesting по компонентам: `{ "wizard": { "next": "Next", "back": "Back", ... }, "results": { ... }, ... }`.
   - Плюральные формы где применимо: `{ "projects": { "count": "{{count}} saved", "count_other": "{{count}} saved" } }`.
   - Ошибочные сообщения из `ToastSystem` и error-boundary — туда же.

3. **`ru.json`:**
   - Полный перевод ВСЕХ ключей из `en.json`. Не оставлять пустые/англоязычные строки.
   - Плюральные формы русского: `one` / `few` / `many` / `other`.
   - Терминология: придерживаться стандартного DS-глоссария (эксперимент, метрика, вариант, уровень значимости, мощность, MDE, конверсия, и т.д.). Не транслитерировать.
   - Готовый reference: commit message термины в русских CX-тасках (`2026-04-21-cx-*.md`).

4. **Language switcher в header:**
   - В `app/frontend/src/App.tsx` рядом с theme toggle — dropdown или сегмент-контрол EN/RU.
   - Нажатие меняет i18next language и сохраняет в localStorage (`ab-test:language`).
   - Reload сохраняет выбор.
   - A11y: `role="group"`, `aria-label`, каждая кнопка `aria-pressed`.

5. **Backend `Accept-Language`:**
   - Middleware или dependency в FastAPI: парсит `Accept-Language` (через `babel.Locale.parse` или ручной split), делает доступным через `request.state.language` (или FastAPI `Depends`).
   - `app/backend/app/services/export_service.py` — функции рендера markdown/html/pdf принимают `language: Literal["en", "ru"]`.
   - Строки отчёта — в `app/backend/app/i18n/en.json` и `ru.json` (новая папка, зеркалит ключи фронтенда где релевантно).
   - `app/backend/app/rules/catalog.py` — warning-тексты тоже локализуемые (новый формат: `message_key: "warnings.seasonality_present"` + лукап в i18n-словаре по language).
   - Backend `/api/v1/analyze` — `report` в response содержит локализованный контент.

6. **Тесты:**
   - Frontend: unit-тесты переключения языка (`i18n.test.tsx`) — мок i18next или real `i18next.init` в test, проверить что после `changeLanguage('ru')` рендер содержит русский.
   - Backend: `test_export_api.py` — дополнительный кейс на `Accept-Language: ru` → markdown содержит русский «Резюме», не «Executive summary».
   - A11y тесты (`a11y-*.test.tsx`) должны продолжать проходить.

7. **Regen `api-contract.ts` и `docs/API.md`**: `python scripts/generate_frontend_api_types.py --check` и `generate_api_docs.py --check` = 0.

8. **Один коммит**:
   ```
   feat: full i18n integration with react-i18next and ru translation
   ```

9. **Отчёт `docs/plans/2026-04-22-i18n-report.md`:**
   - Список локализованных компонентов / backend endpoints.
   - Coverage: сколько ключей в en.json / ru.json (должно быть равно).
   - Скриншоты (необязательно, но если Playwright e2e проверяет RU — приложить ссылку на e2e run).
   - Что НЕ локализовано и почему (например, server logs — intentionally English).

## Acceptance
- `scripts\verify_all.cmd --with-e2e` = exit 0 на HEAD после коммита.
- `jq 'keys | length' app/frontend/src/i18n/en.json` = `jq 'keys | length' app/frontend/src/i18n/ru.json` (одинаковое число top-level keys).
- `grep -rn 'data-testid=' app/frontend/src/ | wc -l` — unchanged (не разломали existing тесты).
- `curl -H "Accept-Language: ru" http://127.0.0.1:8008/api/v1/analyze -X POST -d @docs/demo/sample-project.json -H "Content-Type: application/json"` возвращает 200 с русским `report.executive_summary`.
- `npm run test:unit` в app/frontend — 184 + новые i18n-тесты passed.
- Frontend bundle gzip main < 140 KB (небольшой growth допустим из-за i18next; если > 140 — подумать про lazy-load translations).
- Lighthouse a11y ≥ 0.9 (language switcher не должен сломать скор).
- Commit subject уникальный, `Co-Authored-By: Codex <noreply@anthropic.com>`.
- Этот CX-файл стадж в тот же коммит.
- `git status --short` = пусто.

## How
1. Baseline: `git status --short` = пусто, `scripts\verify_all.cmd` = 0.
2. Добавить deps: `npm install --prefix app/frontend react-i18next i18next i18next-browser-languagedetector`. Не обновлять lock-file целиком — только для новых.
3. Переписать `src/i18n/index.ts` на полную инициализацию.
4. Скриптом или руками вытянуть строки в `en.json`. Подсказка: `grep -rn '>[A-Z][a-z]' app/frontend/src/components/` найдёт большую часть JSX-строк.
5. Перевести всё в `ru.json`. Проверить плюрализм на числовых строках (`"{{count}} saved"` → `one/few/many`).
6. Language switcher в `App.tsx`.
7. Backend: создать `app/backend/app/i18n/en.json` и `ru.json`, middleware, обновить `export_service.py` и `rules/catalog.py`.
8. Тесты. Regen контрактов.
9. Прогон: `scripts\verify_all.cmd --with-e2e`.
10. Commit + report.

## Notes
- **CX-файл hygiene:** staging этот файл в свой коммит.
- **Commit subject hygiene:** проверка на дубль перед финалом (`git log --oneline -15 | ... uniq -d` пусто).
- **НЕ** занижать lighthouse пороги ради прохождения.
- **НЕ** использовать runtime-fetch локалей — оба файла бандлятся в initial chunk через import (они малы).
- **НЕ** менять signatures публичных API — `Accept-Language` optional, default en.
- **Bundle budget:** если main JS gzip перевалит 140 KB — вынести `ru.json` в lazy-chunk (`import("./i18n/ru.json")` при смене языка). Основной chunk содержит только en.
- **Качество перевода:** если сомневаешься в термине — оставь английский в угловых скобках рядом `"Эксперимент <experiment>"` и зафиксируй в отчёте для code review. Лучше помеченный перевод, чем неточный.
- **НЕ** трогать CHANGELOG v1.0.0 / RELEASE_NOTES — это post-v1.
- Backend `test_performance` может флапнуть — перезапустить один раз.
- **НЕ** пушить на remote.

## Out of scope
- de.json / es.json / другие языки
- RTL languages (ar/he)
- Server-side translation of LLM advice (остаётся на языке модели)
- Date/number formatting per locale (отдельный таск при необходимости)
- Documentation translation (README/docs/) — остаются en
