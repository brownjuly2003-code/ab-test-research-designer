# Optional differential-statistics oracle dependencies.
# Install after requirements-dev.txt in a separate CI/manual job. These packages are
# intentionally absent from requirements.txt and the production image.
numpy==2.5.1
pandas==2.3.2
scipy==1.17.1
statsmodels==0.14.6
lifelines==0.30.3
