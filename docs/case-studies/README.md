# Case studies

Reproducible case-study inputs and outputs captured from backend endpoints live here.

- `checkout-redesign.json` stores the calculation response, design response, interim observations, and Bayesian posterior probabilities for the README checkout example.

Rerun:

```bash
python scripts/generate_case_study_numbers.py > /tmp/cs.md
```
